Deque:
Menu selection allows for single integer only (getch).
Enqueue allow up to 10 integers per number.

Quicksort:
Console will prompt for input,
"Enter order:" input of 0(ascending) or 1(descending) only.
"Enter size of array:" positive integer only.
"Enter elements:" integers followed by a space.